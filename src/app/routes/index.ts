import express from "express";

const router = express.Router();

// const moduleRoutes = [
//     {
//         path: "/geography",
//         route: GeographyRoutes_v1,
//     }
// ];

// moduleRoutes.forEach((route) => router.use(route.path, route.route));

export default router;
